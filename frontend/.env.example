# Backend API URL (leave empty to use default http://localhost:8000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# NOTE: OPENAI_API_KEY should NOT be stored in frontend!
# It is managed securely on the backend via environment variables.
# The backend handles all OpenAI API calls.
